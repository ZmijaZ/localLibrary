<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <h1><%= title %></h1>

    <p><%= genre.name %></p>

    <% if(booksOfGenre.length){ %>

    <p>
      <strong
        >Delete the following books before attempting to delete this
        genre.</strong
      >
    </p>

    <div>
      <h4>Books</h4>

      <dl>
        <% for(let book of booksOfGenre){ %>
        <dt><a href="<%= book.url %>"><%= book.title %></a></dt>
        <dd><%= book.summary %></dd>
        <% } %>
      </dl>
    </div>

    <% } else { %>

    <p>Do you really want to delete this Genre?</p>

    <form action="" method="POST">
      <input type="hidden" name="genreid" required value="<%= genre._id %>" />
      <button type="submit">Delete</button>
    </form>

    <% } %>
  </body>
</html>
