<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <h1><%= title %></h1>

    <p><%= bookinstance._id %></p>

    <p>Do you really want to delete this Instance of a book?</p>

    <form action="" method="POST">
      <input
        type="hidden"
        name="bookinstanceid"
        required
        value="<%= bookinstance._id %>"
      />
      <button type="submit">Delete</button>
    </form>
  </body>
</html>
