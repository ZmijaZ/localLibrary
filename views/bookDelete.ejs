<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <h1><%= title %></h1>

    <p><%= book.title %></p>

    <% if(bookInstances.length){ %>

    <p>
      <strong
        >Delete the following instances of the book before attempting to delete
        this book.</strong
      >
    </p>

    <div>
      <h4>Book instances</h4>

      <dl>
        <% for(let instance of bookInstances){ %>
        <dt><a href="<%= instance.url %>"><%= instance._id %></a></dt>
        <dd><%= instance.status %></dd>
        <% } %>
      </dl>
    </div>

    <% } else { %>

    <p>Do you really want to delete this Book?</p>

    <form action="" method="POST">
      <input type="hidden" name="bookid" required value="<%= book._id %>" />
      <button type="submit">Delete</button>
    </form>

    <% } %>
  </body>
</html>
